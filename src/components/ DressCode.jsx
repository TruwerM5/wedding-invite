
// eslint-disable-next-line react/prop-types
export default function DressCode ({ code }) {
    return (
        <div className="dresscode rounded-full" style={{backgroundColor: code}}>
        </div>
    );
}